
# 智慧自然語言助理導入計畫書

**專案版本：** v1.0
**日期：** 2025年11月5日

---

## 1. 專案背景與目標

### 1.1. 背景
目前，公司的物料管理系統雖然功能強大，但使用者在查詢資料、執行特定操作時，仍需依賴傳統的表單、篩選器和多層級的選單。這不僅操作效率較低，新進員工也需要一定的學習時間才能熟練使用，無形中增加了操作成本和時間成本。

### 1.2. 目標
本計畫旨在為現有系統導入一個「智慧自然語言助理」功能，達成以下核心目標：

*   **提升操作效率：** 讓使用者能透過日常對話般的語言，直接下達指令，如「查詢上個月的撥料紀錄」、「幫我找出 A 物料的缺料狀況」等，大幅縮短操作路徑。
*   **降低使用門檻：** 消除複雜介面的學習曲線，讓所有使用者都能直觀、快速地與系統互動。
*   **實現個人化助理：** 系統能根據不同使用者的身份，提供個人化的工作清單，例如主管能快速看到待簽核的單據。
*   **奠定智慧化基礎：** 為未來導入更進階的 AI 功能（如需求預測、異常檢測）建立穩固的技術基礎。

---

## 2. 方案評估與比較

為了在滿足「純內部網路運行」的前提下，找到最佳技術路徑，我們評估了以下兩種核心方案：

| 比較項目 | 方案一：本地規則引擎 (spaCy) | 方案二：自架設大型語言模型 (Ollama) |
| :--- | :--- | :--- |
| **功能強大度** | 中等。能處理明確、基於規則的指令。 | **極高**。能理解複雜、模糊語意，並支援自我學習。 |
| **網路需求** | **純內部網路** (僅需一次性下載模型) | **純內部網路** (僅需一次性下載模型) |
| **硬體成本** | **無** (可沿用現有伺服器) | **極高** (需額外採購高階 GPU 伺服器) |
| **軟體/使用成本** | **無** (開源免費) | **無** (開源免費) |
| **技術門檻** | 中等。需開發人員編寫規則。 | **高**。需要專業人員維護 GPU 環境與模型。 |
| **資料安全性** | **極高**。 | **極高**。 |

---

## 3. 建議方案：分階段實施

考量到成本、風險和快速驗證價值的需求，我們強烈建議採用**分階段實施**的策略：

**第一階段：採用「方案一：本地規則引擎」進行原型開發與驗證。**

*   **理由：**
    1.  **低風險、低成本：** 無需任何額外硬體投資，即可啟動專案。
    2.  **快速實現：** 能在短時間內開發出核心功能，讓使用者快速體驗，驗證此方向的商業價值。
    3.  **滿足核心需求：** 完全符合在純內部網路運行的硬性要求。
    4.  **奠定基礎：** 此階段開發的前後端框架，未來可無縫升級至更強大的 AI 模型。

**第二階段：根據第一階段成果，評估升級至「方案二：使用 Ollama 自架 LLM」。**

*   **理由：**
    當第一階段的系統證明其價值，且使用者提出了更進階的智慧化需求後，我們便有充分的數據和理由，向管理層申請硬體預算，升級到功能更強大的自架設 LLM，以解鎖「自我學習」、「複雜語意理解」等高階功能，同時繼續保持 100% 的資料內網安全性。

---

## 4. 專案範圍與實施計畫 (第一階段)

### 4.1. 範圍
*   **後端：** 開發基於 spaCy 的自然語言解析器、API 端點、以及將解析結果轉換為資料庫查詢的邏輯。
*   **前端：** 在現有介面中，開發一個非侵入式的「智慧搜尋框」。
*   **初期功能：** 優先實現 5-10 個高價值、基於明確規則的查詢指令。例如：
    *   依物料名稱/編號查詢申請單。
    *   查詢指定日期的撥料紀錄。
    *   查詢個人待處理的任務。
    *   查詢特定物料的目前庫存。

### 4.2. 實施步驟與預估時程
1.  **環境設定 (1-2 天):** 安裝 spaCy，並在內網伺服器部署中文語言模型。
2.  **後端開發 (5-7 天):** 開發 API 介面與核心的規則解析邏輯。
3.  **前端開發 (3-4 天):** 開發搜尋框介面與前後端互動邏輯。
4.  **整合與測試 (2-3 天):** 進行前後端整合測試與內部壓力測試。
5.  **上線與回饋收集 (持續):** 內部上線，並建立管道收集使用者回饋。

**預估總時程：約 3-4 週完成第一階段的開發與部署。**

---

## 5. 資源需求 (第一階段)

*   **人力資源：** 1 名 Python/Django 後端開發人員。
*   **硬體資源：** 無需額外採購。
*   **軟體資源：** 所有需要的軟體均為開源免費。

---

## 6. 預期效益

*   **直接效益：** 使用者查詢資料的平均時間預計可**縮短 50% 以上**。
*   **間接效益：**
    *   降低新進員工的系統學習成本。
    *   提升使用者滿意度與工作效率。
    *   為公司培養 AI 應用開發經驗，建立未來智慧化的基石。

---

## 7. 風險評估 (第一階段)

*   **主要風險：** 「本地規則引擎」的智慧程度有限，可能無法理解部分使用者的模糊或複雜提問。
*   **緩解措施：**
    1.  在功能上線初期，明確告知使用者目前系統的能力範圍。
    2.  積極收集使用者「希望 AI 聽得懂，但 AI 目前聽不懂」的語句，作為後續規則優化的依據，以及評估是否進入第二階段的重要參考。

---
---

# 附錄：方案詳細分析

## 方案一：本地規則引擎 (使用 spaCy)

此方案的核心是，在現有的應用程式中，加入一個基於關鍵字和語法規則的解析引擎，以理解使用者的指令。

### 優點：

1.  **極低的硬體成本：**
    完全不需要採購新的伺服器或任何特殊硬體。此方案基於 CPU 運算，可以直接運行在您現有的 Django 伺服器上。
2.  **零軟體與使用成本：**
    spaCy 是一個開源且完全免費的 Python 函式庫，運行在您自己的伺服器上，因此不會產生任何 API 使用費或持續性的軟體授權費用。預算清晰且可控。
3.  **高速的反應速度：**
    對於已定義好的規則，處理速度極快，通常在毫秒級別即可完成。因為所有運算都在本地完成，沒有任何網路延遲。
4.  **部署簡單快速：**
    對於開發人員來說，這就像是為專案加入一個新的 Python 依賴套件。整合過程相對簡單，能快速開發出第一版原型。
5.  **完全離線運行：**
    在首次下載完語言模型後，整個系統即可在完全封閉的內部網路中運行，不與外界有任何資料交換，能提供最高的資料安全性。

### 缺點：

1.  **理解能力有限：**
    這不是真正的人工智慧。它更像一個複雜的「關鍵字比對」系統，難以處理同義詞、上下文、或使用者較為口語化和模糊的提問。例如，它可能無法單獨理解「那些快用完的螺絲」和「庫存很少的螺絲」是同一個意思。
2.  **高昂的維護成本：**
    系統的「智慧」完全依賴於開發人員編寫的規則。每當需要增加一種新的查詢方式或理解一個新的詞彙，都必須由開發人員修改程式碼、測試並重新部署。系統的知識庫僵化且擴充不易。
3.  **無學習能力：**
    系統無法從與使用者的互動中學習和進步。我們之前討論的「教導 AI」或「線上自我修正」等高階功能，在此方案下是無法實現的。
4.  **功能天花板低：**
    雖然處理簡單查詢很有效，但若要擴展到更複雜的意圖（例如：分析數據、自動產生多步驟的報告），程式碼的複雜度會急遽增加，變得難以維護。

### 建議需求設備：

*   **伺服器：** 無需新增。可直接部署於現有的 Django 應用程式伺服器。
*   **CPU：** 標準的伺服器級 CPU 即可 (例如：Intel Xeon, AMD EPYC)，無特殊要求。
*   **記憶體 (RAM)：** 建議 16 GB 或以上。spaCy 本身佔用資源不多，但為 Web 應用整體保留充裕的記憶體是好的實踐。
*   **GPU：** 完全不需要。
*   **儲存空間：** 額外需要約 500MB ~ 1GB 的硬碟空間，用於存放 spaCy 的中文語言模型檔案。

---

## 方案二：自架設大型語言模型 (使用 Ollama)

此方案的核心是，在公司內部建立一台專門的 AI 伺服器，運行一個強大的開源大型語言模型，為應用程式提供真正的 AI 理解能力。

### 優點：

1.  **極強的語意理解能力：**
    能真正理解人類語言的複雜性、模糊性和上下文關聯。使用者可以用最自然、最口語化的方式下達指令。
2.  **支援線上學習與進化：**
    可以實現「教導 AI」的機制。系統的智慧可以透過與使用者的互動持續成長，變得越來越聰明，越來越貼近業務需求。
3.  **低廉的維護成本：**
    系統的「商業邏輯」大多儲存在 Prompt（提示）文字中。當需要調整 AI 的行為時，很多時候只需要修改這些提示文字，而無需修改程式碼，維護起來更靈活、更快速。
4.  **功能天花板極高：**
    由於其強大的理解和推理能力，可以輕鬆擴展到各種複雜的任務，如：數據洞察分析、自動化報告生成、跨資料表的複雜查詢等。
5.  **最高的資料安全性：**
    與方案一相同，所有運算和資料都 100% 在公司內部處理，是兼顧了強大功能與頂級安全性的方案。

### 缺點：

1.  **極高的硬體購置成本：**
    這是此方案最大的挑戰。需要投入一筆可觀的資金，一次性採購一台或多台配備高階 GPU 的專用伺服器。
2.  **較高的能源與維護成本：**
    GPU 伺服器是「吃電怪獸」，長期的電費是一筆持續的營運成本。同時也需要合適的機房環境（如散熱、不斷電系統）來維護。
3.  **需要專業技術能力：**
    雖然 Ollama 大幅簡化了軟體設定，但管理一台生產環境的 GPU 伺服器（包含驅動程式更新、硬體監控、故障排除等）仍需要比普通網頁伺服器更高的專業技術能力。
4.  **反應速度依賴硬體：**
    AI 回應的速度直接取決於 GPU 的運算能力。如果 GPU 不夠強大，使用者在下達指令後可能會感受到明顯的等待延遲。

### 建議需求設備：

*   **伺服器：** 必須新增一台獨立的、為 AI 運算設計的 GPU 伺服器。
*   **GPU (核心關鍵)：** 建議選擇 NVIDIA 的高階 GPU。GPU 的 VRAM (顯示記憶體) 大小，直接決定了您能運行多大規模、多聰明的 AI 模型。
    *   **入門推薦級 (可流暢運行中小型模型)：** 1 x NVIDIA RTX 4090 (24GB VRAM) 或 RTX 3090 (24GB VRAM)。
    *   **企業推薦級 (可運行大型模型，效能更佳)：** 1 x NVIDIA L40S (48GB VRAM), A100 (80GB VRAM), 或 H100 (80GB VRAM)。
*   **CPU：** 搭配伺服器級的多核心 CPU (例如：Intel Xeon, AMD EPYC)。
*   **記憶體 (RAM)：** 建議 128 GB 或以上。
*   **儲存空間：** 建議 1 TB 或以上的 NVMe SSD。高速硬碟能顯著加快 AI 模型的載入和啟動速度。
